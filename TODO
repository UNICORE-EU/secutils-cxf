1) check if W3CDOMStreamReader2 is still needed

2) Soap v12 checks are removed/disabled in WSSecHeader

Expected downstream changes:

 - removed ReliableProxy class (xfire specific, maybe other solution will be introduced)
 